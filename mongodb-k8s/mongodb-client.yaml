apiVersion: v1
kind: Pod
metadata:
  name: mongo-client
  labels:
    app: mongo-client
spec:
  containers:
  - name: mongo-client
    image: mongo:6.0
    command:
    - sleep
    - "3600"
    stdin: true
    tty: true

